/**
 * Branches — TEMPORARY BRIDGE: coming in S3 slice.
 */
import MuiEmptyState from "../components/reusable/MuiEmptyState.jsx";

const Branches = () => (
  <MuiEmptyState
    title="Branches"
    description="Coming in S3 slice"
  />
);

export default Branches;