/**
 * Dashboard — TEMPORARY BRIDGE: proves the guarded session loop
 * until the §49 dashboard slice replaces it.
 */
import MuiEmptyState from "../components/reusable/MuiEmptyState.jsx";

const Dashboard = () => (
  <MuiEmptyState
    title="Dashboard"
    description="Coming in §49 slice"
  />
);

export default Dashboard;